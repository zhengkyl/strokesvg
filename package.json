{
  "name": "strokesvg",
  "version": "1.0.0",
  "scripts": {
    "dev": "http-server",
    "build": "svgo --recursive -f ./src -o ./dist"
  },
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "svgo": "^3.0.2"
  }
}